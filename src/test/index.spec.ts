const promiseTest = (): Promise<number> =>
  new Promise((resolve, reject) => {
    resolve(1);
  });

describe('Example async/await test', () => {
  xit('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
  it('awaits promise', async () => {
    const x = await promiseTest();
    expect(x).toEqual(1);
    expect(x).toEqual(1);
    // assert.equal(x, 1);
  });
});
